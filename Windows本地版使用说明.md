# 小红书笔记截图工具 Windows 本地版

## 第一次使用

1. 安装 Google Chrome。
2. 安装 Python 3.12，安装时勾选 `Add python.exe to PATH`。
3. 安装 Node.js 20 或更新版本。
4. 解压 `小红书笔记截图工具_Windows本地版.zip`。
5. 双击 `安装依赖.bat`。
6. 双击 `启动截图工具.bat`。

浏览器会自动打开：

```text
http://127.0.0.1:8788/
```

## 以后使用

直接双击 `启动截图工具.bat` 即可。

## 说明

- 本地版使用电脑上的 Chrome 打开笔记并截图。
- 不登录、不保存账号、不处理验证码。
- 结果文件和失败现场截图会保存在同目录的 `runs` 文件夹。
- 如果端口被占用，先关闭之前打开的命令行窗口，再重新启动。
